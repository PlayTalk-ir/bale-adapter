# Cline (VS Code)

1. Workspace = repo root.
2. Enable terminal integration.
3. System prompt:

```
Read .ai/startup.md first.
Activate before CLI: . .\scripts\activate.ps1
Entry: python scripts/support_cli.py
Agent id: YOUR_NAME
```

Use same commands as `.ai/design/cli-surface.md`.
